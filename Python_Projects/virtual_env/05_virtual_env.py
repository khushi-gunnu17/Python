# almost different computer can be established from it


# https://virtualenv.pypa.io/en/latest/cli_interface.html




'''
commands for creating a virtual environment inside a folder

pip install virtualenv
virtualenv .venv  (venv = name of the virtual env folder, can be given any name, but the standard practice is to give .venv only.)

pip list 
python -m pip install django
pip list > requirement.txt
pip list 

for deactivation command : .venv\Scripts\deactivate

'''
